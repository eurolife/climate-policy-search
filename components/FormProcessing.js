import Spinner from './Spinner';

const FormProcessing = () => {
  return (
    <div className="bg-whitetrans absolute inset-0 flex items-center justify-center">
      <Spinner />
    </div>
  )
}
export default FormProcessing;